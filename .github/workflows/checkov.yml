---
name: Checkov

# Actions are pinned to full commit SHAs (the trailing `# vX.Y.Z` is the tag
# that SHA corresponded to) and every tool to an exact version -- no floating
# tags. Version bumps are therefore explicit, reviewable changes to this file.
#
# Only GitHub-hosted runners and public actions are used. This is deliberate
# and is why these workflows live on the public branches only: internal CI runs
# on self-hosted runners that do not exist for this repository on github.com.
#
# Soft-fail for the initial rollout: findings publish to Security -> Code
# scanning instead of blocking the branch. Tighten the `exit-code` /
# `--soft-fail` / `continue-on-error` switches once the backlog is triaged.

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'release-[0-9]+\.[0-9]+'
  pull_request:
    branches:
      - main
      - 'release-[0-9]+\.[0-9]+'
    types: [opened, synchronize, reopened, ready_for_review]
  schedule:
    # Weekly re-scan so new findings surface without a code change.
    # NOTE: GitHub only fires `schedule` on the repository's default
    # branch, so this trigger stays dormant until these workflows are
    # also merged to `main`. The push and pull_request triggers above
    # work on every listed branch regardless.
    - cron: '40 4 * * 1'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checkov:
    name: Checkov (Kubernetes / Actions / secrets)
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Harden the runner (audit all outbound calls)
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12.14'

      - name: Install Checkov
        run: pip install --disable-pip-version-check "checkov==3.3.16"

      # `helm` is deliberately not in --framework: Checkov renders charts into a
      # random temp dir and reports paths relative to it, so every finding points
      # at a file that does not exist in the repo -- unanchorable in the Security
      # tab, and re-reported as new on every run.
      #
      # The 47 `templates/` directories are skipped for the complementary
      # reason: as raw files they are Go-templated text, not Kubernetes YAML, so
      # the `kubernetes` framework cannot parse them. Between those two rules
      # Checkov does not report on the chart bodies at all -- Helm
      # misconfiguration coverage is Trivy's job in trivy.yml, which renders
      # charts through its own Helm parser and anchors findings at real paths.
      #
      # NOTE: unlike the inference repository this workflow was adapted from,
      # `charts/` is NOT skipped. Its only entry,
      # deployment/components/edp/charts/edp-redis, is first-party (a
      # dependency-only Redis chart written here), not a vendored upstream
      # chart, so its defaults ARE ours to fix.
      - name: Run Checkov
        continue-on-error: true
        run: |
          checkov -d . \
            --framework kubernetes github_actions secrets \
            --skip-path '.*/templates/.*' \
            --soft-fail --compact --quiet \
            -o sarif --output-file-path console,. || true
          ls -la results.sarif 2>/dev/null || echo "no results.sarif produced"

      - name: Upload Checkov SARIF
        uses: github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        if: always() && hashFiles('results.sarif') != ''
        with:
          sarif_file: results.sarif
          category: checkov
